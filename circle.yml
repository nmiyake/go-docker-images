machine:
  services:
    - docker

test:
  override:
    - cd alpine-go-1.6 && docker build .
    - cd alpine-go-1.7-1.8 && docker build .
    - cd alpine-go-1.7.5 && docker build .
    - cd brew-go && docker build .
    - cd go-1.7-1.8 && docker build .
    - cd go-1.8-rpm-fpm && docker build .
